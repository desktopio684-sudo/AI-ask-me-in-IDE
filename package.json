{
    "name": "quizgate-workspace",
    "version": "1.0.0",
    "private": true,
    "workspaces": [
        "shared",
        "packages/*"
    ],
    "scripts": {
        "build": "npm run build --workspaces --if-present",
        "test": "npm test --workspaces --if-present",
        "test:quiz": "bash scripts/test-quiz.sh"
    },
    "devDependencies": {
        "typescript": "^5.3.3",
        "@types/node": "^20.10.0"
    }
}