{
    "name": "@quizgate/shared",
    "version": "1.0.0",
    "description": "Shared types for QuizGate MCP and VS Code Extension",
    "main": "dist/types.js",
    "types": "dist/types.d.ts",
    "scripts": {
        "build": "tsc",
        "watch": "tsc -w"
    },
    "dependencies": {
        "zod": "^3.22.4"
    },
    "devDependencies": {
        "typescript": "^5.3.3"
    }
}